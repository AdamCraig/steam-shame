import { pricing } from 'steam-info/helpers/pricing';
import { module, test } from 'qunit';

module('Unit | Helper | pricing');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pricing([42]);
  assert.ok(result);
});
