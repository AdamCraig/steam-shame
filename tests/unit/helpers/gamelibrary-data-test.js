import { gamelibraryData } from 'steam-info/helpers/gamelibrary-data';
import { module, test } from 'qunit';

module('Unit | Helper | gamelibrary data');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = gamelibraryData([42]);
  assert.ok(result);
});
