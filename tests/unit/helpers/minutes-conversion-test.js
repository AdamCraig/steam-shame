import { minutesConversion } from 'steam-info/helpers/minutes-conversion';
import { module, test } from 'qunit';

module('Unit | Helper | minutes conversion');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = minutesConversion([42]);
  assert.ok(result);
});
